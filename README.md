# 1-Bit-Full-Subtractor
## Introduction
This project presents the design and implementation of a 1-bit full subtractor using SystemVerilog. A full subtractor is a combinational logic circuit that computes the difference of two single-bit binary numbers while also accounting for a borrow-in from a previous (less significant) stage, producing both a difference bit and a borrow-out bit. The objective of this project is to develop the full subtractor structurally, by instantiating two half-subtractor modules and combining their outputs with a single OR gate, rather than implementing the logic behaviorally from a truth table. SystemVerilog is employed for the implementation, and a self-checking testbench is developed to validate the design against all eight possible input combinations. In conclusion, the full subtractor correctly reproduces the expected difference and borrow-out for every input combination, demonstrating how a full subtractor can be systematically built from two half-subtractor building blocks.

# What is a Full Subtractor
A full subtractor is a combinational digital circuit that subtracts one 1-bit binary number from another while also taking into account a borrow bit from a previous subtraction stage. This makes it different from a half-subtractor, which can only handle two inputs and cannot accept a borrow-in, making it unsuitable for chaining together to build multi-bit subtractors. A full subtractor is a fundamental building block used in ALUs (Arithmetic Logic Units), multi-bit subtractor circuits, and digital signal processing datapaths. The full subtractor implemented in this project produces a difference output (diff) and a borrow-out (bout) for the three inputs: minuend (a), subtrahend (b), and borrow-in (bin).

## Design and Implementation
### Full Subtractor Architecture
The full subtractor is implemented structurally, using two instances of a half-subtractor module along with a single 2-input OR gate:

Half-Subtractor 1 — computes an intermediate difference (a − b) and an intermediate borrow, using inputs a and b
Half-Subtractor 2 — computes the final difference by subtracting bin from the intermediate difference, and generates a second intermediate borrow
OR Gate — combines the two intermediate borrow signals to produce the final borrow-out (bout)
This structural approach mirrors how full subtractors are conventionally taught and built in hardware, reusing a smaller, verified module rather than duplicating combinational logic.

Tech Stack
Vivado ML Edition - 2025.2 used to implement and simulate the code.

Working
The working of the full subtractor involves cascading two half-subtractors and combining their borrow outputs. The step-by-step operation is given below:

Input Stage:

The three 1-bit inputs a (minuend), b (subtrahend), and bin (borrow-in) are provided to the module.
First Half-Subtractor:

Computes an intermediate difference d1 = a XOR b.
Computes an intermediate borrow bw1, which is high whenever a is smaller than b.
Second Half-Subtractor:

Takes d1 and bin as inputs.
Computes the final difference diff = d1 XOR bin.
Computes a second intermediate borrow bw2, which is high whenever d1 is smaller than bin.
Combining Borrows:

The final borrow-out bout is generated by OR-ing bw1 and bw2, since a borrow is required if either half-subtractor stage needed one.
This is purely combinational — no clock is required.
Port Description
Port	Width	Direction	Description
a	1-bit	Input	Minuend
b	1-bit	Input	Subtrahend
bin	1-bit	Input	Borrow-in from a previous stage
diff	1-bit	Output	Difference bit (a − b − bin)
bout	1-bit	Output	Borrow-out to the next stage
Results and Discussion
In this project, a 1-bit full subtractor is implemented in SystemVerilog by structurally instantiating two half-subtractor modules and combining their borrow outputs with an OR gate. A self-checking testbench exhaustively verified the design against all 8 possible combinations of a, b, and bin, comparing the simulated diff and bout against expected values computed independently in the testbench. All output values matched the expected results for every combination. The following waveform was observed after simulation:

Waveform
Waveform

RTL Schematic
RTL Schematic

Conclusion
In conclusion, the project successfully designed a 1-bit full subtractor in SystemVerilog using a structural, two-half-subtractor architecture. The design correctly computes the difference and borrow-out for all eight possible input combinations, and the self-checking testbench confirms functional correctness without requiring manual waveform inspection. The design is entirely combinational, requiring no clock signal, which makes it suitable for use inside ALUs and multi-bit subtractor chains. The 1-bit full subtractor holds potential applications in arithmetic units, multi-bit binary subtractors formed by cascading multiple stages, and any system requiring bit-level subtraction with borrow propagation. The structural, module-reuse approach makes the design easy to verify and extend.

References
Javatpoint. Full Subtractor in Digital Logic. Retrieved from https://www.javatpoint.com
Wikipedia. Subtractor. Retrieved from https://en.wikipedia.org/wiki/Subtractor
Vivado Design Suite User Guide. Retrieved from https://www.xilinx.com/support/documentation/sw_manuals/xilinx2023_1/ug973-vivado-release-notes-install-license.pdf
Youtube. Full Subtractor using Half Subtractors. Retrieved from https://www.youtube.com/watch?v=example
